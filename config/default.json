{
  "host": "localhost",
  "port": "APP_PORT",
  "mariadb": "DB_URL",
  "forceDbSync": "DB_FORCE_SYNC",
  "auth": {
    "idField": "id",
    "token": {
      "secret": "FEATHERS_AUTH_SECRET"
    },
    "local": {}
  }
}
